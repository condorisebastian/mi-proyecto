name: app_usuario
description: Aplicación para estudiantes, civiles y adultos mayores - Sistema de transporte Santa Cruz
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.0.0
  http: ^1.1.0
  shared_preferences: ^2.2.0
  qr_flutter: ^4.1.0
  flutter_nfc_kit: ^3.0.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
